import React from 'react';

import { Container } from './styles';

const Mensagens: React.FC = () => {
  return (
    <Container>
      <span>Mensagens</span>
    </Container>
  );
}

export default Mensagens;